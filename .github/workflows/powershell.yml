name: PowerShell checks
on:
  push:
  pull_request:
permissions:
  contents: read
jobs:
  isolated-tests:
    runs-on: windows-latest
    strategy:
      matrix:
        shell: [powershell, pwsh]
    steps:
      - uses: actions/checkout@v4
      - name: Parse scripts and run isolated tests
        shell: ${{ matrix.shell }}
        run: ./tests/Run-Tests.ps1
